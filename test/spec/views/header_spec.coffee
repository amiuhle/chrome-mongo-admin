describe "Views.Header", ->
  view = null
  callback = null

  # it "fires a ready event when done", ->
  #   runs ->
  #     view = new mb.Views.Header
  #     callback = jasmine.createSpy "callback"
  #     view.on "ready", callback
    
  #   waitsFor ->
  #     callback.wasCalled
  #   , "Callback should be called", 100


  